[ws1-network](../../index.md)/[com.airwatch.gateway.clients](../index.md)/[AWWebView](index.md)/[cleanUp](clean-up.md)

# cleanUp

```kotlin
open fun cleanUp()
```

Cleans up the proxy configuration and headers. This should be called when the WebView is no longer needed to ensure that the proxy settings do not affect other WebView instances or network requests.
